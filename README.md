# Junos Cool&Chic

Static website with drawings, journals, an advent calendar gallery, and a few browser games.

## Local preview

Run from this directory:

```bash
./start.sh
```

Then open `http://localhost:4173`.

## Notes

- This `public-release` copy was prepared as a fresh publishable export without the private repository history.
- GitHub Pages is configured from the repository root.
